import 'package:flutter/material.dart';

class ClaimTypes {
  final String title;
  final int count;
  final Color bgColor;
  final Color textColor;

  ClaimTypes({this.title, this.count, this.bgColor, this.textColor});
}
